{
  "name": "workflow-platform",
  "version": "0.6.3",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --import ./tests/hermetic.mjs --test",
    "lint": "node src/cli.mjs lint",
    "packages:generate": "node scripts/generate-packages.mjs",
    "packages:check": "node scripts/generate-packages.mjs --check"
  }
}
